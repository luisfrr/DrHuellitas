@using DrHuellitas.BO;
@model RegistrosBO
@{
    ViewBag.Title = "Comercio";
    Layout = "~/Views/Shared/MasterFrontEnd.cshtml";
}
<div class="main" role="main">
    <div id="content" class="content full">
        <div class="container">
            <div class="row">
                <div class="col-md-12 img-thumbnail" id="repetir">
                            <div class="col-md-4" id="Modal">

                            </div>
            </div>
        </div>
    </div>
</div>
 </div>
<script>
    //escrip para las imagenes
     document.getElementById("slider").style.height = "200px"
    $("#slider").attr("data-pagination", "");
    $("#slider").attr("data-arrows", "");
    $("#slider").attr("data-style", "");

    //escrip para cargar un buen de cosas
    $.get("/Usuario/jsonpropaganda", null, DataBind);
    function DataBind(lista) {
        var SetData = $("#repetir");
        for (var i = 0; i < lista.length; i++) {
            var data ="</br>"+ "<div class='card' row_" + lista[i].id + ">" +
                "<div class='container-fliud'>" +
                "<div class='wrapper row'>" +
                "<div class='preview col-md-6'>" +
                "<img src=" + lista[i].foto + "  style='width:200px; height:150px;' class='center-block'/>" +
                " </div>" +

                "<div class='details col-md-6'>" +
                "<h3 class='product-title'>Datos de la veterinaria" + lista[i].id + "</h3>" +
                " <div class='rating'>" +
                "<p class='product-description'>Nombre de la veteria:" + lista[i].descripcion + "</p>" +
                "<p class='product-description'>Nombre del gerente:</p>" +
                "<p class='product-description'>Email de la veterinari:</p>" +
                "<p class='product-description'>Telefono de la veterinari:</p>" +
                "<h3 class='product-title'>Servicios que ofrecen:</h3>" + "<br />" + "<br />" +
                "<h3>Imagen de la empresa</h3>" +
                "<img src="+lista[i].foto+"  style='width:200px; height:150px;' class='center-block'/>" +
                "</div>" + "<br />" +
                "<div class='action'>" +
                "<button class='add-to-cart btn btn-default center-block' type='button'>Ver comentarios</button>" +
                "</div>" +"</br>"+

                "</div>" + "</br>" +
                "</div>" + "</br>" +
                "</div>" + "</br>" + 

                "</div>"+"</br>";
            SetData.append(data);
        }

    }
</script>

